# CALO-RPD Studio 1.0.8

This release corrects the Live Optimization empty-state behavior. The default Automatic metric shows best normalized constraint violation until all currently monitored algorithms have produced feasible incumbents, then displays best-feasible objective convergence. Completed experiment histories are restored from persistent result metadata when the live page is opened after a run.
