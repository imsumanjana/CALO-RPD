#!/usr/bin/env python3
"""Repository-local launcher for the AI intelligence tooling."""
from pathlib import Path
import runpy
import sys

ROOT = Path(__file__).resolve().parents[1]
sys.argv[0] = str(ROOT / "tools" / "ai_repo_intelligence.py")
runpy.run_path(sys.argv[0], run_name="__main__")
