# The CI environment must exercise the exact CPU runtime graph, not a second resolution of it.
-c requirements-lock-cpu-py311-linux.txt
-r requirements-container.in

# Release-validation tooling is resolved into one Linux/Python 3.11 hash lock.
pytest>=8,<10
pytest-qt>=4.4,<5
pytest-cov>=5,<7
coverage>=7,<8
ruff==0.15.22
mypy>=1.13,<2
build>=1.2,<2
setuptools>=70
wheel>=0.45
uv==0.11.29
