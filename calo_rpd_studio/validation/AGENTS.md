# Validation instructions

- Independent validation is authoritative and must recompute from stored decoded candidates.
- Record convergence, constraint residuals, lattice validity, scenarios, and objective discrepancy.
- A mismatch beyond tolerance invalidates the candidate and is never silently repaired.
