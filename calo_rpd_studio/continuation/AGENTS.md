# Continuation instructions

- Preserve exact scientific state, policy identity, schema versions, FE accounting, and deterministic replay.
- Reject incompatible continuation artifacts without mutation.
- Never describe an OOM restart or backend change as exact continuation unless its invariants prove that claim.
